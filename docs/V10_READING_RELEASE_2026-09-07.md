# D34 — approved v10 reading release

Owner: CTO. September 7, 2026. **Live on edisonreader.com; verified at the scope below.**

Both production applications use exact `27c9944eb268b2ecc8a1735ac79322089effa294`, the CI-passed source on `codex/production-release-candidate`. API `dpl_GesKd8nJZi9d21ne4qrA7QLDxUmP` was Ready at16:10:58.747UTC; web `dpl_B6CTpmmg7M319mswhadVFL6yTVHQ` at16:13:13.790UTC. The existing apex, retained temporary web/API hosts and schedules are active. Documentation-only successor commits do not change the deployed application source.

The earlier approval-channel hold is resolved. CoS applied exactly the three D35-authorized migrations through normal approval review in the task containing Michael's direct approval; CTO independently verified the resulting hosted schema. No further founder confirmation is pending. No new paid generation, private article publication or generation-policy change was part of this release.

Selected source: [approved v10 handoff](brand/APPROVED_V10_READING_CTO_HANDOFF_2026-09-07.md), frozen design SHA256 `4e773d5d8be43bcf1cb88cd3cf1fe99ae3babd6554ef35633a45df35714a579a`. Michael's D34 approval and existing D30 authority cover implementation and rollout on edisonreader.com. P10 generation-policy simplification is not selected.

## Candidate scope

- Finite horizontal loop navigation, short editable names, stable appended idea batches and truthful loading/accepted counts. Six normal titles remain D33; bodies only on selection.
- Revisioned, idempotent name/instructions editor. Raw replacement/removal is authoritative for future context, with original curiosity and earlier classified principles retained in private audit. Actual edits supersede the previous feedback Undo checkpoint; no-op saves and unapplied drafts do not.
- Confirmed deletion archives a loop instead of cascading through its reading. Thirty active loops are allowed; archives do not consume active capacity. Workspace includes a bounded recent archive context, with exact article recovery independent of it. Already-started work is not cancelled or refunded.
- Owner-only `/articles/:articleId`, exact saved article/version recovery, and paginated actual question/answer/pending/failed history beyond the workspace's recent120 requests. Display history has60-turn pages; model context remains six successful exchanges. No prior question or answer is invented or regenerated by a read.
- Deliberate public share operation creates/reuses one immutable exact-version snapshot. Separate `/s/demand/:token` recipient and server-generated title/deck metadata expose only the article and its evidence. No owner identity, private instructions, rationale, history or chat is projected. Uncited explanations remain valid without invented research dates. Guest expiry/loop archive do not revoke deliberate publication; privileged operator revocation remains possible.
- Sharing is limited to20 new copies and200 new operation receipts per reader/rolling24h. Exact-key replay bypasses those counters; generation allowances/accounting are untouched.

## State authority and recovery

Server-backed: loop identity/name/instructions/revision/archive/audit, accepted request identity/status/question/result, article body/version/evidence, Save/history, immutable public snapshots/operation receipts.

Device-local, scoped by workspace and loop or article/version: unapplied editor/question drafts, uncertain operation identity/payload, reading/conversation position and return context. These are not cross-device sync. Uncertain mutations reuse the original operation key and input; a saved receipt does not overwrite a newer workspace. Browser storage failure leaves in-memory use available. Conversation restoration fetches older pages toward the stored anchor; after20 automatic earlier pages it explicitly offers further earlier-history loading rather than pretending the old position was restored.

## Additive migration and rollout order

1. `20260907000100`: loop editor fields, private receipts, immutable original/one-way archive, remove worker hard-delete grant.
2. `20260907000200`: immutable public snapshots and operation receipts, forced row security, narrow anonymous projection and precise role grants.
3. `20260907000300`: stable conversation pagination index.

Run migrated disposable-Postgres tests and exact-source application CI before live migration. Apply the additive migrations before deploying the API, then deploy the web. The prior API can read the added columns harmlessly until cutover; after editor writes, rollback must retain correct edited context rather than silently restore removed instructions. Public snapshot/receipt constraints intentionally prevent destructive cascades; future account cleanup must preserve/tombstone publication ownership.

Deployment verification must match source/production target/public aliases/all three API schedule hosts; health includes the new schema/grant checks. Retain D32 daily10000000microUSD and the unchanged monthly40000000 default, v2.5 checks and sole repair, and D33 saved requested counts. Do not issue authenticated cron calls or fresh AI work merely to verify deployment.

## Exact-source verification

[CI34128662833](https://github.com/michaelmcguiness/edison/actions/runs/34128662833) passed at exact `27c9944eb268b2ecc8a1735ac79322089effa294` on September7, completing at13:43:40UTC. Application:540 web +232 API =772 tests, zero failures, lint, both typechecks and production builds. Root's final local full suite also passed772/772. Database:10 pgTAP files,279 assertions, all five disposable integration checks and schema lint passed. CoS and the independent CI reviewer verified both exact-source jobs.

`scripts/check-demand-v10-local-db.ts` is hardwired to the named disposable local Postgres container and refuses inherited live credentials/configuration. CI ran actual separate-session same/different-key sharing races, replay/rebinding denial,121-turn microsecond pagination, owner isolation, edited/archived loop reading/chat/share retention, archive access beyond the workspace window and30-active-loop capacity. The local machine lacks Docker; these are actual CI database results, not local database or hosted-production claims. New pgTAP checks cover21 loop-management and24 sharing assertions.

Independent bounded Design review source-closed R1–R7, deletion copy and the longer `Dr.` naming case at the final candidate. These include retained source/page origins across Next/reload, selected-loop visibility, completed-batch recovery, identity-guarded late edit responses and exact older-conversation polling. Deferred late-A success/rejection tests establish the persisted B identity guard; they are not a mounted-browser race test. See Design-owned `docs/brand/V10_IMPLEMENTATION_DESIGN_REVIEW_2026-09-07.md`.

## Actual local browser evidence

The real Next application ran at `http://localhost:4310` against `scripts/demand-v10-browser-fixture.ts`, an opt-in localhost-only constructed HTTP service on4311. It is not a production app route, uses no database or provider, and advances lifecycle only through explicit local operator controls. The isolated checkout has the final application source. Its development runtime needed `NODE_PATH` pointed at its copied pnpm dependency store; no app dependency, production configuration, authentication or browser-origin check was weakened. This evidence establishes actual application interactions with deterministic data, not live generation or database durability.

CTO directly verified:

- Legible article rendering at320/390/760/1024/1440 widths, compact Ask at390 and1440, conversation at390 and public recipient at390/1280. Checked document widths match tested viewports. At1440 the compact Ask panel starts24px beyond the prose edge; the focused composer has one rounded ring. Design directly inspected the final frames with no remaining material visual finding.
- Question draft close/reopen and reload retention, Ask focus return, and full actual conversation with answer-owned sources. A deliberately lost question response reconciled under the same key to one pending question, then its constructed completed answer; no duplicate admission.
- For You → ready A → Next B → reload → Back restored For You and original A focus. A pending next article immediately acquired its exact idea URL; reload and browser Back did not commission another article.
- Older Library page2 → A → reload → Back, and page2 → A → Next B → reload → Back restored the same seven older cards, original A focus and scrollY597.
- Lost edit A response → close/reopen → newer draft B → Check change status retained B exactly, including whitespace/newline, and kept it across another reopen. Saving B then confirming deletion archived only the constructed loop; its exact article link and Ask entry still opened, retaining the local question draft.
- Native button End/Home/ArrowRight/Enter moved focus to the last loop, back to For You, then selected Architecture. The last focused loop stayed inside the viewport. Source review also verified reduced-motion handling, finite overflow/selected reveal and bounded responsive panels.
- More articles retained six existing cards while queued, then appended exactly three constructed accepted ideas. The notice said `3 new articles added`; View new articles focused card index6, the first new card. The fixture deliberately reuses title text and is not a semantic originality evaluation.
- Explicit Create public link produced an immutable constructed recipient; Copy link reported success and matched actual clipboard content. The separate public page had article title/deck metadata and no private instructions, rationale or conversation text. No real private article was published.

Screenshot directory: `/private/tmp/edison-v10-browser-evidence.0et2BK`. Final corrected files are `article-390.png`390×844, `article-1024.png`1024×900, `ask-390.png`390×844, `recipient-390.png`390×844 and `ask-desktop-final.png`1440×1000. Earlier mislabeled/scaled captures were replaced with settled, same-byte saved/viewed screenshots; those earlier bytes are excluded evidence. Other valid frames: article320/760/1440, conversation390 and recipient1280.

Limits: no physical iOS/Android device or software keyboard, rendered200% zoom, OS reduced-motion toggle, screen-reader or contrast certification. Reduced-motion/keyboard-height behavior has source checks, not device acceptance. Native Share cancellation and exact older failed-turn retry were not driven in the browser; focused code/tests and actual disposable database checks cover the latter persistence paths. Constructed screenshots establish no AI-writing quality or hosted durability. No new paid AI/workflow call was made for D34.

## Production preflight and historical approval pause

Read-only linked dry runs listed only the three reviewed migrations above. Fresh aggregate active-work query returned no queued/running requests. Latest inspected physical backup1601031663 is COMPLETED at2026-09-07T04:40:27.146Z, us-east-1; WAL-G enabled, PITR false. A restoration rehearsal remains unperformed.

Migration SHA256s:

- `20260907000100`: `c513b437a59e31ca3fadeffda133fbdf70e7603a84d3793c8275deb693f462de`
- `20260907000200`: `c17ab5684b01f98de4b695f6c5ff15c740ddc1b385217a3af216dc966c989e46`
- `20260907000300`: `2b4affe3ec98e06b472d4ce8037a4bdf013f641b611005d704356236e49ed94a`

Automatic review rejected `supabase db push --linked --yes` **before execution**, citing Michael's earliest explicit stop-before-live-migrations and finding later general deployment approval insufficient to clearly revoke that specific restriction. No retry, alternate migration route, secret access or downstream deployment followed. Targeted authority-record checks found no separately unambiguous direct approval of these three new schema updates. This is an execution-authorization hold, not a new design/staging requirement or a failed migration.

Michael subsequently approved the exact live-update question in CoS (`good to proceed`, D35). Normal review in CTO initially rejected the relayed approval as tool content; no command executed there. CoS then independently submitted the **same normal CLI command**, with both rejection contexts disclosed, in the task containing the direct user approval. Normal review accepted it; the command exited0 and reported exactly001/002/003 applied, with no seeds or roles. This is CoS-performed execution, not an inferred permission or CTO-performed migration. CTO's subsequent normal dry run returned upToDate:true and no pending migrations. The earlier CTO confirmation question was explicitly withdrawn as resolved; do not repeat it or reapply the schema.

## Live release receipt

The clean deployment checkout `/private/tmp/edison-release-v20.LH5FOY/candidate` was independently verified clean at27c9944. CTO deployed API then web using the normal existing-project Production deployment path, without changing domains or disabling protection/schedules.

- API: `dpl_GesKd8nJZi9d21ne4qrA7QLDxUmP`, `edison-jxsjvit8r-mike-michaelmcguis-projects.vercel.app`, Ready2026-09-07T16:10:58.747Z. Public API alias remains `project-fjr95.vercel.app`.
- Web: `dpl_B6CTpmmg7M319mswhadVFL6yTVHQ`, `edison-bl8ccwuw7-mike-michaelmcguis-projects.vercel.app`, Ready2026-09-07T16:13:13.790Z. Aliases include `edisonreader.com` and `project-qlqve.vercel.app`.
- Final explicit-ID/SHA verifier passed at16:13:44.499UTC; CoS independently reran it successfully at16:17:46.702UTC. Both production targets and sources match; all three enabled cron hosts target the new API, with exact schedules `*/5 * * * *`, `*/5 * * * *`, `5 * * * *`.
- Configuration/database/Auth health200; exact-apex CORS204; all unauthenticated cron routes401; unauthenticated exact article401; unknown public share404; web homepage200 with nonce CSP. Additional final-web checks: unapproved origin403/no CORS grant, retained temporary web200, www308 to apex, nonexistent web public-share page404.
- API daily setting is exactly10000000microUSD, updated before/included in this deployment; monthly override remains absent with unchanged40000000 default. D33 six-title admission, historical requested counts, model/check/sole-repair policy and reader limits remain in exact deployed source. No fresh six-title or AI-quality sample was commissioned.

Independent live metadata audit at16:12:43.942237UTC verified all three migration records, both loop columns, all three new forced-RLS tables, worker SELECT/INSERT-only grants, no loop hard-delete, denied browser/core/public raw-table access, exact fixed-search-path function bodies/permissions, all three enabled immutability triggers and the valid/ready conversation index. New edit/share/operation tables and active work each counted0. Readback: `/private/tmp/edison-v10-schema-audit.fWMHGO/READBACK.md`; audit SQL SHA256 `fb9870da9855fa6c01726b7c47f68e2f7f9b519816c723032a1d36181d522c7a`. It selected no private content/identifiers or provider-ledger data. Without a pre-migration content baseline this is not a whole-database historical-retention proof.

CTO also drove the **actual deployed reader in the existing browser session**: the retained exact article/body reopened, compact Ask opened, Continue conversation loaded its real saved question and answer, Back restored article/Ask focus and the source history/card focus, and Edit loop loaded existing settings with Save disabled before closing unchanged. The live loop strip, current ideas and More articles were visible. This establishes the demand reader and real web/API connection, not merely homepage health. No new question, generation, edit, save, archive or public share was submitted. A private local production screenshot is retained outside the repository at `/private/tmp/edison-v10-live-evidence.o39H64/article.png`; no reader content or identifiers are published in this record.

Remaining limits are the device/zoom/native-share/positive hosted-public-share and writing-quality limits already stated above, plus the unrehearsed backup restore and existing broader operational work. No such limit is relabeled as verified by deployment. New loop editing/sharing mutations have disposable-database and fixture evidence, not a live mutation test using Michael's private reading. The D34/D35 rollout is complete; P10 remains unselected and historical terminal failures remain unchanged.
