# D34 — approved v10 reading release

Owner: CTO. September 7, 2026. **Implemented; release verification in progress.**

Selected source: [approved v10 handoff](brand/APPROVED_V10_READING_CTO_HANDOFF_2026-09-07.md), frozen design SHA256 `4e773d5d8be43bcf1cb88cd3cf1fe99ae3babd6554ef35633a45df35714a579a`. Michael's D34 approval and existing D30 authority cover implementation and rollout on edisonreader.com. P10 generation-policy simplification is not selected.

## Candidate scope

- Finite horizontal loop navigation, short editable names, stable appended idea batches and truthful loading/accepted counts. Six normal titles remain D33; bodies only on selection.
- Revisioned, idempotent name/instructions editor. Raw replacement/removal is authoritative for future context, with original curiosity and earlier classified principles retained in private audit. Actual edits supersede the previous feedback Undo checkpoint; no-op saves and unapplied drafts do not.
- Confirmed deletion archives a loop instead of cascading through its reading. Thirty active loops are allowed; archives do not consume active capacity. Workspace includes a bounded recent archive context, with exact article recovery independent of it. Already-started work is not cancelled or refunded.
- Owner-only `/articles/:articleId`, exact saved article/version recovery, and paginated actual question/answer/pending/failed history beyond the workspace's recent120 requests. Display history has60-turn pages; model context remains six successful exchanges. No prior question or answer is invented or regenerated by a read.
- Deliberate public share operation creates/reuses one immutable exact-version snapshot. Separate `/s/demand/:token` recipient and server-generated title/deck metadata expose only the article and its evidence. No owner identity, private instructions, rationale, history or chat is projected. Uncited explanations remain valid without invented research dates. Guest expiry/loop archive do not revoke deliberate publication; privileged operator revocation remains possible.
- Sharing is limited to20 new copies and200 new operation receipts per reader/rolling24h. Exact-key replay bypasses those counters; generation allowances/accounting are untouched.

## State authority and recovery

Server-backed: loop identity/name/instructions/revision/archive/audit, accepted request identity/status/question/result, article body/version/evidence, Save/history, immutable public snapshots/operation receipts.

Device-local, scoped by workspace and loop or article/version: unapplied editor/question drafts, uncertain operation identity/payload, reading/conversation position and return context. These are not cross-device sync. Uncertain mutations reuse the original operation key and input; a saved receipt does not overwrite a newer workspace. Browser storage failure leaves in-memory use available. Conversation restoration fetches older pages toward the stored anchor; after20 automatic earlier pages it explicitly offers further earlier-history loading rather than pretending the old position was restored.

## Additive migration and rollout order

1. `20260907000100`: loop editor fields, private receipts, immutable original/one-way archive, remove worker hard-delete grant.
2. `20260907000200`: immutable public snapshots and operation receipts, forced row security, narrow anonymous projection and precise role grants.
3. `20260907000300`: stable conversation pagination index.

Run migrated disposable-Postgres tests and exact-source application CI before live migration. Apply the additive migrations before deploying the API, then deploy the web. The prior API can read the added columns harmlessly until cutover; after editor writes, rollback must retain correct edited context rather than silently restore removed instructions. Public snapshot/receipt constraints intentionally prevent destructive cascades; future account cleanup must preserve/tombstone publication ownership.

Deployment verification must match source/production target/public aliases/all three API schedule hosts; health includes the new schema/grant checks. Retain D32 daily10000000microUSD and the unchanged monthly40000000 default, v2.5 checks and sole repair, and D33 saved requested counts. Do not issue authenticated cron calls or fresh AI work merely to verify deployment.

## Verification evidence and remaining work

Current local evidence:744 automated application tests passed before final UI recovery regressions; both typechecks and lint subsequently passed. Focused owner/cursor/privacy/revision tests and independent backend source review passed. Final exact-source totals will be recorded at completion.

`scripts/check-demand-v10-local-db.ts` is hardwired to the named disposable local Postgres container; it refuses inherited live credentials/configuration. It checks actual separate-session sharing races, exact replay,121-turn microsecond pagination, owner isolation, archive/reading/chat/share retention, and active-loop capacity. The local machine lacks Docker, so actual DB execution is an explicit CI gate, not a claimed local result. New pgTAP checks cover21 loop-management and24 sharing assertions.

`scripts/demand-v10-browser-fixture.ts` is an opt-in localhost-only synthetic HTTP service, never an app route or production backend. It has no provider/database calls, uses constructed reading and125 prior questions, and changes lifecycle only through explicit local test controls. Browser evidence against it establishes actual application interaction/rendering with deterministic data, not live generation or database durability.

Still required: exact-source CI/migrations; desktop/phone/keyboard/zoom/reduced-motion/return/reload/recipient verification and independent Design review; production rollout and read-only health/access checks. No v10 deployment or new paid sample is established by this preparation record.
