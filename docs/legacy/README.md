# Retired OpenAI Sites configuration

The original Edison prototype was registered as an OpenAI Site. The project ID
is retained here so the identity can be recovered, but this repository no
longer targets the Vinext/Cloudflare/D1 runtime. The production architecture is
documented in `ARCHITECTURE.md`.
