-- Production-like seed data is intentionally empty.
--
-- Create local users through Supabase Auth so the auth.users trigger builds the
-- matching profile and preferences. Seeded articles are generated by the local
-- development script once an authenticated user exists.
